Для 64 бит JVM

Один объект без полей занимает 16 байт.

Объект с полями занимает 72.

Установлены минимальные значения -Xmx3m -Xms1m

Для 220 объектов (около 0,015 Мб) виртуальная машины вызывает сборщик мусора самостоятельно.